#include<bits/stdc++.h>

using namespace std;

int main() {
    vector<int> arr;
    int n = arr.size();
    int j = 10;
    for (int i = 0; i < n; i++) {
        if (arr[i] > arr[j]) cout << "Hello World";
    }
    return 0;
}
