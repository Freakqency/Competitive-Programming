#include <bits/stdc++.h>

using namespace std;


int main() {
    string s = "hello";
    int n = s.length();
    string p = "hlo";
    int count[256] = {0};
    for (int i = 0; i < n; i++)
        cout << count[i] ;
}
