#include<bits/stdc++.h>

using namespace std;

void solve(int n, vector<int> arr, int k) {
    int res = 0;
    int curr = 0;
    int start = 0'
}

int main() {
    vector<int> arr{};
    int n = static_cast<int>(arr.size()), k = 8;
    solve(n, arr, k);
    return 0;
}
