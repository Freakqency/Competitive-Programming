#include <bits/stdc++.h>

using namespace std;

int solve(int n) {
}

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);    
    int n = -1;
    while (n != 0) {
        cin >> n;
        if (n == 0)
            break;
        cout << solve(n) << endl;
    }
    return 0;
}
